name: promptflow-sdk-cli-test
on: 
  pull_request:
    paths:
      - '**blank.yml'
      - 'README.md'
permissions:
  id-token: write
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: exit -3
